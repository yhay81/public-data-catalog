# Contributing

このリポジトリは、AIが公開データを安全に発見・比較・利用開始できることを重視します。

## 1件の登録に必要な情報

- 一意な `id`
- サービス名、提供者、対象分野、地域、言語
- APIまたはデータポータルの公式URL
- 認証方式、利用可能な形式、レート制限
- ライセンスまたは利用条件の公式URL
- `ai_summary`、`query_hints`、`caveats`
- `last_verified`（最後に公式情報を確認した日）

## 記述ルール

- `catalog.json` のみを正本として編集する。READMEの表は項目追加時に更新する。
- 公式ドキュメントを優先し、検索結果ページや第三者のまとめを正本にしない。
- 「APIキー不要」「商用利用可」などの強い表現は、公式の利用条件で確認できた場合だけ書く。
- データセットごとに条件が異なる場合は、`varies_by_dataset` または `varies_by_record` と明記する。
- APIキー、アクセストークン、個人のメールアドレスなどの秘密情報をコミットしない。
- エンドポイントは可能な限りHTTPSを使い、秘密値を含むクエリ文字列を登録しない。
- 生データを大量にミラーせず、公式ソースへのリンクと取得方法を記録する。

## 確認手順

```sh
python3 scripts/validate_catalog.py
git diff --check
```

項目を更新した場合は、なぜ変更したかと、公式ソースのどこで確認したかをコミットまたはプルリクエストに残してください。
