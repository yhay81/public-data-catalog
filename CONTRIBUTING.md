# Contributing

このリポジトリは、AIや開発者が公開データを安全に発見・比較し、根拠付きの最初の結果を再現できることを重視します。
目的、非目標、調査ループは [PURPOSE.md](./PURPOSE.md) に、価値の判断基準は [docs/value-redefinition.md](./docs/value-redefinition.md)、調査の判断記録は [docs/investigation-log.md](./docs/investigation-log.md) にあります。

情報源の件数は成功指標ではありません。貢献は、可能な限り「誰のどの問いを再現可能にするか」から始めてください。

参加時は [Code of Conduct](./CODE_OF_CONDUCT.md) を守り、脆弱性は [Security Policy](./SECURITY.md) に従って報告してください。

## レシピを提案する

Issueの `Retrieval recipe request` を使い、次を示してください。

- 対象となる具体的な問い
- その問いを必要とする利用者またはワークフロー
- 候補となる公式情報源
- 期待する値、単位、識別子、出典
- 認証、ライセンス、改訂、品質、プライバシー上の制約

同じ問いに既存レシピで答えられる場合は、新規追加より既存レシピの改善を優先します。

## レシピを追加する

1. [レシピ仕様](./docs/recipe-format.md) と既存レシピを読む
2. `recipes/<id>.json` を1ファイル追加する
3. 必要な情報源が未登録なら `catalog.json` に情報源プロファイルを追加する
4. 公式ドキュメント、利用条件、ライセンスを確認する
5. 最小リクエストを実行し、主要フィールドと解釈を確認する
6. 検証コマンドをすべて実行する
7. 小さな結果と公式根拠をプルリクエストに記録する

原則として、1つのプルリクエストには1つの問いを含めてください。

## レシピの必須情報

- 一意な `id`
- 日本語・英語のタイトルと具体的な問い
- `catalog.json` に存在する `source_id`
- HTTPS、GET、許可ホスト、最大応答サイズ
- HTTP status、content type、主要フィールドのassertion
- 抽出する値、単位、コード、識別子
- 定義、欠損、速報・確報、改訂、比較上の注意
- 公式ソースURL、ライセンスURL、コピー可能な出典表記
- 実際にレシピ全体を確認した `last_verified`

JSON Pointerと安全制約の詳細は [docs/recipe-format.md](./docs/recipe-format.md) にあります。

## 情報源プロファイルの必須情報

- 一意な `id`
- サービス名、提供者、対象分野、地域、言語
- APIまたはデータポータルの公式URL
- 認証方式、利用可能な形式、レート制限
- ライセンスまたは利用条件の公式URL
- `ai_summary`、`query_hints`、`caveats`
- `last_verified`（最後に公式情報を確認した日）

利用シナリオやレシピに接続されない情報源を、分野の穴埋めだけを理由に追加しないでください。

## 記述ルール

- 情報源は `catalog.json`、レシピは `recipes/*.json` を正本として編集する。
- 公式ドキュメントを優先し、検索結果ページや第三者のまとめを正本にしない。
- 公式ソースで確認できない項目は推測で埋めず、`varies_by_dataset` などの不確実性を明記する。
- 「APIキー不要」「商用利用可」などの強い表現は、公式の利用条件で確認できた場合だけ書く。
- データセットごとに条件が異なる場合は、`varies_by_dataset` または `varies_by_record` と明記する。
- APIキー、アクセストークン、個人のメールアドレスなどの秘密情報をコミットしない。
- レシピはHTTPSの読み取り専用GETに限定し、秘密値を含むクエリ文字列を登録しない。
- 広い日付範囲、全件取得、不要な列、過剰なページサイズを避ける。
- 生データを大量にミラーせず、公式ソースへのリンクと取得方法を記録する。
- 数値を固定assertionにせず、正当な改訂と契約破壊を区別できる識別子や構造を検査する。
- `last_verified` は、文書を読んだ日ではなく、完全なレシピを実行した日に更新する。

## 確認手順

```sh
python3 scripts/validate_catalog.py
python3 -m unittest discover -s tests -v
python3 scripts/recipe_tool.py check <recipe-id>
git diff --check
```

ネットワークを使うprobeが実行できない場合は、理由と手動確認方法をプルリクエストに明記してください。項目を更新した場合は、なぜ変更したか、公式ソースのどこで確認したか、どの応答を検証したかを残してください。

## レビュー方針

レビューでは件数より、次を優先します。

1. 問いが具体的で、実際の利用価値があるか
2. 公式ソースと利用条件に基づいているか
3. リクエストが小さく、安全で、再現可能か
4. 値を誤解しないための文脈が残るか
5. 継続的に検証できる保守コストか
